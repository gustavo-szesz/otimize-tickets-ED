# Copilot Instructions

## Current project state

This repository is currently empty (no source files, build scripts, or documentation were found yet).

## Build, test, and lint commands

No project commands are defined at the moment.

When project tooling is added, keep this section updated with:
- Full build/test/lint commands
- The command for running a single test (file, spec name, or test ID)

## High-level architecture

No architecture can be inferred yet because no application code is present.

Update this section once the first modules are added, focusing on:
- Main runtime entry points and execution flow
- Integration boundaries (APIs, jobs, webhooks, external systems)
- Data movement between major components

## Key conventions

No repository-specific coding conventions are defined yet.

As conventions emerge, document only patterns that are specific to this codebase (naming, folder boundaries, integration contracts, error-handling patterns, etc.).
